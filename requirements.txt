# ============================================
#  Python 依赖（可选，用于高级分析）
# ============================================
# analysis.py 使用纯标准库，无需安装任何依赖
# 以下包为可选增强（安装后可使用更高级的统计功能）

# 基础数据处理
numpy>=1.24.0
pandas>=2.0.0

# 统计分析
scipy>=1.10.0
statsmodels>=0.14.0

# 机器学习（可选）
scikit-learn>=1.3.0

# 时间序列分析（可选，用于增强 ARIMA）
arch>=6.2.0

# 安装方法：
# pip install -r requirements.txt
