# 2026-04-16 工作日志

## 登录系统升级（类 MetaMask 模态框）

- 新增 `src/components/AuthModal.tsx`：优质登录/注册模态框，含 Tab 切换、密码显示、错误动画、最近账号快选
- 升级 `src/hooks/useWeb3.ts`：改为 modalOpen 状态驱动，不再使用 window.prompt；登录成功自动写 adminToken（admin 角色）
- 升级 `src/components/Navbar.tsx`：账号头像 + 下拉菜单（复制/切换账号/退出），类 MetaMask 风格，按用户名生成渐变色头像
- 升级 `src/App.tsx`：接入 AuthModal 组件

## 管理员控制台重写（2026-04-16 晚）

- 全面重构 `src/pages/Admin.tsx`：移除 window.prompt、移除 bootstrap 初始化区块、移除重复的钱包签名登录
- 管理员身份从 AuthModal 的 admin 登录自动继承（localStorage adminToken）
- 新增 4 个 Tab：市场审核 / 结算管理 / AI 配置 / 机器人
- 新增统计栏：市场总数/待审核/待结算/已结算
- 新增快速创建市场功能
- AI 功能全连接：题目合规检测（AI review）+ 联网检索结果判定（AI settle）
- AI 配置面板：支持三档模式切换（纯人工/AI辅助/纯AI）+ 联网检索开关 + 多模型启用
- App.tsx 同步更新：Admin 接收 userId prop

- 服务运行在 http://localhost:3000，前后端合并在 server.ts（Vite middleware 模式）
- FABRIC_ENABLED=false，使用 SQLite Mock 模式
- GEMINI_API_KEY 已配置
- 管理员账号：admin / admin123
